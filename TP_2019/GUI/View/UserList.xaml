<UserControl x:Class="GUI.View.UserList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GUI.View"
             mc:Ignorable="d" Width="781.925" Height="415.444">
    <Grid>
        <ListView HorizontalAlignment="Left" Height="365" VerticalAlignment="Top" Width="750" Margin="10,10,0,0">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="250" Header="Imię" DisplayMemberBinding="{Binding FirstName}" />
                    <GridViewColumn Width="250" Header="Nazwisko" DisplayMemberBinding="{Binding LastName}" />
                    <GridViewColumn Width="250" Header="Telefon" DisplayMemberBinding="{Binding Telephone}" />
                </GridView>
            </ListView.View>
        </ListView>
        <Button Grid.ColumnSpan="1" Content="Edytuj" Command="{Binding EditUserCommand, UpdateSourceTrigger=PropertyChanged,  Mode=TwoWay}" HorizontalAlignment="Left" Margin="263,380,0,0" VerticalAlignment="Top" Width="103" Height="25" RenderTransformOrigin="-1.24,13.852"/>
        <Button Content="Nowy" Command="{Binding AddUserCommand, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" HorizontalAlignment="Left" Margin="10,380,0,0" VerticalAlignment="Top" Width="103" Height="25"/>
        <Button Content="Usuń" Command="{Binding DeleteUserCommand, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" HorizontalAlignment="Left" Margin="130,380,0,0" VerticalAlignment="Top" Width="103" Height="25"/>

    </Grid>
</UserControl>
